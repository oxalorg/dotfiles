alias cdd='cd /c/Users/ox'
alias tk='cd /c/Users/ox/Dropbox/Projects/taazakitchen/'
alias drop='cd /c/Users/ox/Dropbox'
# Needs to be DONE
export EDITOR=nvim
export VISUAL=nvim
alias e=$EDITOR
alias et="$EDITOR /projects/todo/todo.txt"
alias ett="$EDITOR /projects/todo/completed.txt"
export MAIL=$HOME/mail/INBOX

# MPC MPD Aliases
alias mpn='mpc next'
alias mpt='mpc toggle'
alias mpp='mpc prev'
alias mppl='mpc playlist'
alias mpa='mpc add'

# List commands
# alias ls="ls -GpF"
# alias ll="ls -lGpF"
alias lt="ls -ltGpF --color=tty"
# alias la="ls -aGpF"

# Git
alias gst='git status -b'
alias ga='git add'
alias gu='git add -u'
alias gcm='git commit'
alias gacm='git add -u && git commit'
alias gp='git push'
alias gpu='git pull'
alias gc='git checkout'
alias gb='git branch'
alias gd='git diff .'
alias gdc='git diff --cached .'
alias gac='git add -u && git commit'

## Git log
alias gl='git log --graph --color --decorate --all --stat -p'
# alias gls="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias gls="git log --graph --full-history --all --oneline --decorate --abbrev-commit"
alias glss="git log --graph --full-history --all --oneline --decorate --abbrev-commit --simplify-by-decoration"

# Stylize commands
alias grep='grep --color'
alias nyancat='pygmentize -g -O style=colorful'

# Utils
alias dude='du -hcd 1 | sort -hr'
alias t='tree'

# Custom sed
alias sedmdimg="sed 's/\(.*\)/![\1]\(\1\)/g'"

# Custom directories
alias cab='cd /cabinet'
alias www='cd /cabinet/www'
alias doc='cd /doc'
alias proj='cd /projects'
ccd() {
    cd $OLDPWD
}

# quickedits
alias envim='$EDITOR $HOME/.config/nvim/init.vim'
alias ezshrc='$EDITOR $HOME/.zshrc'
alias ealiases='$EDITOR $HOME/.aliases'

# Super useful shortcuts
alias agg='ag -g'
alias agga='ag -g ""'
alias agd='find . -type d | ag'
alias -g AG='| ag'
alias -g G='| grep'
alias ftd='find . -type d'

alias -g lenv='env $(cat .env | xargs)'

# Apt
alias sai='sudo apt install -y'

# alias f='rg --files --smart-case | fzf'

alias sai='sudo apt install -y'

alias ghi='gh issue create -t'

alias pir='pip3 install -r requirements.txt'
alias pi='pip3 install'
alias pf='pip3 freeze'
